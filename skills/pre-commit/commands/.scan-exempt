# Pre-commit skill defines the secret patterns it scans for
api[_-]?key
secret[_-]?key
access[_-]?token
password
AWS_SECRET
# Skill documents the --no-verify bypass and steers users away from it
no-verify
--no-verify
